Hi,

This is an image processing project with small amount of data.

Code accounts to classify the images whether they left/right/ no hand

This is still in progreee as it requires large data to train. Categorical cross entropy is used.

Image augnebtation is used to generate more data to train the model.

